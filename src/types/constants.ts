export interface Constants {
  brand: {
    KICK_COLOR: string;
    KICK_IMAGE: string;
    BAN_COLOR: string;
    BAN_IMAGE: string;
    UNBAN_COLOR: string;
    UNBAN_IMAGE: string;
    WARN_COLOR: string;
    WARN_IMAGE: string;
    MUTE_COLOR: string;
    MUTE_IMAGE: string;
    UNMUTE_COLOR: string;
    UNMUTE_IMAGE: string;
  };
  modlogs: {
    colors: {
      kick: string;
      ban: string;
      unban: string;
      warn: string;
      mute: string;
      unmute: string;
    };
    images: {
      kick: string;
      ban: string;
      unban: string;
      warn: string;
      mute: string;
      unmute: string;
    };
  };
  milliseconds: {
    YEAR: number;
    MONTH: number;
    WEEK: number;
    DAY: number;
    HOUR: number;
    MINUTE: number;
    SECOND: number;
  };
}
